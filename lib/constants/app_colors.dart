

import 'package:flutter/material.dart';

const primaryColor = Colors.deepOrange; // main color whole app
const bgColor = Color(0xff191A22);  // main bg color whole app
const containerColor = Color(0xff22232E); // main container color upr bg whole app
const fontColor = Color(0xffFFFFFF); // main font color whole app
const lableColor = Color.fromARGB(255, 142, 139, 139); // main lable text color whole app


// const lableColor = Color(0xff767676); // main lable text color whole app

// const primryColor = Color(0xff00DA64);